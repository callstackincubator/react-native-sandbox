pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{js,ts,jsx,tsx}'
      run: bun eslint {staged_files}
    types:
      glob: '*.{js,ts,jsx,tsx}'
      run: bun typecheck
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: bun commitlint --edit
